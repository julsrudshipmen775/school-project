function addStudentRecord(name) {
    // Add student record to an array
}

addStudentRecord("Alice");

function removeStudentRecord(studentId) {
    // Remove student record from the array based on student ID
}

removeStudentRecord(123);

function updateStudentRecord(id, name) {
    // Update a specific student record with new name
}
