  <script>
    function getRandomInt(max) {
      return Math.floor(Math.random() * max);
    }

    const randomNumber = getRandomInt(10);

    console.log(`This is a simple school project repository for sharing project files and collaborating with classmates.`);
  </script>