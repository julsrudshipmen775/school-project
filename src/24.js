function getStudentNames() {
  // Return student names from database or API call
}

function addStudent(name) {
  // Add new student to database or API call
}

function checkStudentAge(studentId, age) {
  // Check if a student is eligible based on their ID and age
}
